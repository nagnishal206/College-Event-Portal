kind = "web"
previewPath = "/"
title = "College Event Portal"
version = "1.0.0"
id = "artifacts/event-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21436

[services.development]
run = "python app.py"

[services.production]
build = [ "echo", "no build step" ]
run = "gunicorn -b 0.0.0.0:21436 -w 2 app:app"

[services.env]
PORT = "21436"
BASE_PATH = "/"
